workflows:
  expo-ios-unsigned:
    name: Expo iOS Unsigned Build
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      node: 20                  # Sürümü düz '20' olarak güncelledik, sistem hata vermeyecektir
      xcode: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
    scripts:
      - name: Install pnpm
        script: |
          # Node 20'nin aktif olduğundan emin oluyoruz
          nvm use 20 || nvm install 20
          
          # pnpm kurulumu
          curl -fsSL https://get.pnpm.io/install.sh | sh -
          export PNPM_HOME="$HOME/Library/pnpm"
          export PATH="$PNPM_HOME:$PATH"
          echo "export PNPM_HOME=\"$HOME/Library/pnpm\"" >> $CM_ENV
          echo "export PATH=\"$PNPM_HOME:\$PATH\"" >> $CM_ENV
          
      - name: Install dependencies
        script: |
          nvm use 20
          pnpm config set frozen-lockfile false
          pnpm install --reporter=default
      
      - name: Expo Prebuild (Generate Native iOS Project)
        script: |
          nvm use 20
          pnpm exec expo prebuild --platform ios
          
      - name: Build iOS Application (Unsigned)
        script: |
          cd ios
          xcodebuild -workspace *.xcworkspace \
            -scheme $(xcodebuild -list -json | json Workspace.name 2>/dev/null || echo "Hata: Scheme bulunamadı") \
            -configuration Release \
            -archivePath $HOME/build/UnsignedApp.xcarchive \
            CODE_SIGN_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            clean archive
            
      - name: Export IPA (Unsigned)
        script: |
          mkdir -p $CM_EXPORT_DIR
          cp -r $HOME/build/UnsignedApp.xcarchive/Products/Applications/*.app $CM_EXPORT_DIR/
          
    artifacts:
      - $CM_EXPORT_DIR/*.app
